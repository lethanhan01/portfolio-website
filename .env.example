# Copy this file to `.env` (do NOT commit `.env`)
# Used by:
# - Vercel serverless function: /api/send-email
# - Optional local Express server: server/index.ts

# Gmail address used to send mail
FOLIO_EMAIL=your_email@gmail.com

# Gmail App Password (recommended) or SMTP password
FOLIO_PASSWORD=your_app_password_here

# Optional: comma-separated recipients. Defaults to FOLIO_EMAIL if omitted.
# FOLIO_TO_EMAILS=you@gmail.com,other@gmail.com
