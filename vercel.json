{
  "version": 2,
  "installCommand": "npm ci",
  "buildCommand": "mkdir -p dist && npm run build && test -f dist/index.html",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
