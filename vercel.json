{
  "version": 2,
  "installCommand": "npm ci",
  "buildCommand": "mkdir -p public && npm run build && test -f public/index.html",
  "outputDirectory": "public",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
